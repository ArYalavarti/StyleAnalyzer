# StyleAnalyzer
A style analyzing Ruby script that displays all unused CSS classes and ids in a web development
project.


## Build/Run Specifications
Ensure the latest version of Ruby is installed with RVM. Run `bundle install` to install all required gems to
run StyleAnalyzer.

To run, enter `ruby StyleAnalyzer.rb` in your working directory to start the CLI.
